VOIPMS_USER=your@email.com
VOIPMS_PASS=your_api_password

# Traefik basicAuth — generate with: htpasswd -nb username password
# Dollar signs must be doubled in docker-compose labels but NOT here as an env var.
DASHBOARD_AUTH=username:$apr1$xxxxxxxxx$hashedpassword
